name: nordvpn-linux # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: "0.6" # just for humans, typically '1.2+git' or '1.3.2'
summary: Be warned, this app will make connecting to NordVPN as easy as on windows and macos. # 79 char long summary
description: |
  Be warned, this app will make connecting to NordVPN as easy as on windows and macos. It uses Electron and NordVPN-cli under the hood.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
